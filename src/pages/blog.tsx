import { Box, Heading } from "@chakra-ui/react";

const Blog: React.FC = () => {
  return (
    <Box>
      <Heading mt={3} size={"lg"} textAlign={"center"}>
        Welcome to Blog Page
      </Heading>
    </Box>
  );
};

export default Blog;
